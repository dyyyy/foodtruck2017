<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.foodtruck.dao.MemberDAO">

	<!-- 회원 정보 -->
	<select id="getMember" resultMap="memberMap">

		SELECT * FROM MEMBER
		<where> MEM_ID=#{memberId} </where>
	
	</select>

	<!-- 회원 리스트 -->
	<select id="getMemberList" resultMap="memberMap">

		SELECT * FROM MEMBER

	</select>
	
	<!-- 회원 등록 -->
	<insert id="insertMember">

		INSERT INTO MEMBER
		(MEM_ID, MEM_PW, MEM_NAME, MEM_TEL, MEM_AUTH)
		VALUES
		(#{memberId}, #{memberPw}, #{memberName}, #{memberTel}, #{memberAuth})

	</insert>

	<!-- 회원 수정 -->
	<update id="updateMember">

		UPDATE MEMBER
		SET MEM_PW=#{memberPw}, MEM_NAME=#{memberName},
		MEM_TEL=#{memberTel}
		<where> MEM_ID=#{memberId} </where>

	</update>

	<!-- 회원 삭제 -->
	<delete id="deleteMember">

		DELETE FROM MEMBER
		<where> MEM_ID=#{memberId}</where>

	</delete>
	
	<!-- 사업자 번호 -->
	<select id="getLicenseNo" resultMap="SellerMap">
		
		SELECT SELLER.SEL_ID, SELLER.SEL_NAME, LICENSE.LICENSE_NO
		FROM SELLER
		INNER JOIN LICENSE
		ON SELLER.SEL_ID = LICENSE.SEL_ID
		<where> SELLER.SEL_ID=#{selId} </where>
		
	</select>

	<resultMap type="com.foodtruck.vo.MemberVO" id="SellerMap">
		<result column="SEL_ID" property="memberId"/>
		<result column="SEL_NAME" property="memberName"/>
		<result column="LICENSE_NO" property="licenseNo"/>
	</resultMap>
	
	<resultMap type="com.foodtruck.vo.MemberVO" id="memberMap">
		<result column="MEM_ID" property="memberId" />
		<result column="MEM_PW" property="memberPw" />
		<result column="MEM_NAME" property="memberName" />
		<result column="MEM_TEL" property="memberTel" />
		<result column="MEM_AUTH" property="memberAuth" />
	</resultMap>

</mapper>